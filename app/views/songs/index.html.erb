<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% @songs.each do |song| %>
      <p><%= song.name %></p>
      artist:
      <%= song.artist_id %>
      genre:
      <%= song.genre_id %>
    <%end%>
  </body>
</html>
